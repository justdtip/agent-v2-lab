# Integrated pure checks and real-corpus preflight finding

7 September 2026. After isolated merge a29fc75 (landed §14 plus primary guard/atomic-writer changes), the parent ran replay, runtime, regression-resource-preflight and repository-rule tests together. INTEGRATED-PURE-24.log records **102 passed in 17.90 seconds**, exit 0, and MLX_IMPORTED []. This is selected pure integration evidence, not a native/full-suite/scientific acceptance claim. The only merge conflict was two append-only heartbeat histories; both were retained, removing only conflict markers. Upstream patch files have their original blank-context whitespace, which was preserved rather than reformatted. Backup push through a29fc75 succeeded.

Task5a source 793c451 passed independent read-only review and its worker's 26 pure tests. The parent's subsequent check against the actual frozen corpus exposed a production preflight gap: corpus_tokenizer(manifest_path) requires a file with basename tokenizer_config.json, but the corpus descriptor correctly stores resolved Hugging Face blob paths with opaque basenames. It raises ValueError before tokenizer/model loading; no MLX was imported. The original producer worker is fixing that lookup against verified cached snapshot assets, with opaque-blob fixtures and an actual frozen-corpus validation required before rereview. No frozen descriptor or data is changed.

Task5b wrote six failing missing-module tests, then paused source writes for this producer fix. Its record/profile contract remains unchanged. No native test, checkpoint fit, prose capture, or scientific profile ran in this period. The Deputy's accumulation window remains open; a later Codex runtime reservation must respect its end marker as well as the primary lock and mapped-MLX inventory.
