# Resource preflight review and integrated checks — 7 September 2026

Independent reviewer lens_resource_review (GPT-6 Astra) found two source defects at d50ca05: a first calibration peak above the inspected bound but below the device cap still allowed the second callback, and successful solve peak evidence was discarded/cumulative with the last forward.

Worker lens_jacobian fixed both in a separate commit3dc8b10. The initial-bound test was red before the change and now proves the second callback unreachable; a fake main-path test proves peak reset immediately before solving and measured_solve reports measured/projected bytes on success. Ten pure preflight tests pass. Independent scoped rereview marked both findings ADDRESSED with no new material defects. It did not rerun tests or load MLX. Source quality/specification review is resolved; operational memory/timing remain unverified. In particular neither an inspected10GiB allowance nor the sixteen-matrix solve allowance is a proved allocator upper bound.

Primary landed fixes through a6a63dc integrated in merge57c4750, including fork/record guards and the attention-envelope correction. Fresh pure integration on that source: PYTHONPATH=src primary virtualenv python -m pytest -o addopts= -q tests/test_lens_regression_preflight.py tests/test_lens_runtime.py tests/test_repository_rules.py —69 passed in12.86s, exit0. Ruff check --no-cache on preflight script/tests clean. No checkpoint was loaded.

Task3 independent review separately found internal resource-boundary and actual broadcast-width proof gaps. Those are in its Jacobian path, not this regression preflight; its original worker is fixing them. No Jacobian checkpoint work is authorised by this record. Registration for a bounded regression resource measurement is in research/records/LENS-FIT-agentic-regression-2026-09-07/README.md. A sandboxed inventory attempt could not run ps/sysctl; its empty result is NOT a clear slot. An escalated fresh inventory is required before any launch.
