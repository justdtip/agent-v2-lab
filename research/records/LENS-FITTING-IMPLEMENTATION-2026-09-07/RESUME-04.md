# Resume authority — 7 September 2026

Director's answers now appear in requirements §12 and §13, incorporated with corpus landing5712131. Ridge penalty is alpha times total mean diagonal(XTX), retaining the fixed grid; record alpha, mean diagonal and n. The within-trajectory sequence split selects alpha and is not an independent generalisation estimate. Generalisation remains on disjoint pilot episodes.

Replay must resolve `replace(spec, cache_strategy="none")` before loading. The registry's history default is a separate authorised landing6380644, preserved unchanged.

The Director explicitly authorised the GitHub backup push. `git push -u origin codex/lens-fitting` succeeded to https://github.com/justdtip/agent-v2-lab.git. This resolves the prior auto-review egress blocker; no force push was used. The branch then merged5712131 without conflicts. No checkpoint was loaded. A read-only lock/process check after merge found the lock absent and no mapped MLX process; every later native launch must recheck rather than reuse this observation.

No old record has been rewritten. Corpus hashes/membership remain as frozen. The next bounded deliverable is regression source and tests, then its independent review and serial runtime evidence under normal constraints.
