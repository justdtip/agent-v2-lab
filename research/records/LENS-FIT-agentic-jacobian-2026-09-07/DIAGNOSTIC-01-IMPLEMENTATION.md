# Original-full-primal epsilon diagnostic implementation

Implemented only `src/local_llm_lab/pipeline/lens_fitting/epsilon_diagnostic.py`, `scripts/lens_epsilon_diagnostic.py`, and `tests/test_lens_epsilon_diagnostic.py`. The diagnostic uses original frozen row 0, position 438, layers 1/16/31, 16 seeded directions and exact four reference scales and two cached endpoints. The original derivative functions and production proof gates are unchanged. Descriptive records cannot produce a production acceptance or lens output.

The CLI checks the declared 8 GiB against the frozen working set before loading and the actual working set after loading; measured WorkloadMemoryGuard checks run after existing materialized workloads. CLI elapsed budget is 480 seconds including preflight/loading, checked before block calls and stage boundaries. Native calls are not forcibly interrupted. Actual block invocations are hash-chained with shape-derived block-token counts; existing workload guards confirm pending calls after materialization. Failed or incomplete scheduled work is distinguishable. No extra native evaluation boundaries were added. Ledger flushes each record and fsyncs materialization/checkpoints. Responses are persisted as CPU NumPy files; only adjacent CPU responses and one position state remain in memory. Nonfinite response coordinates explicitly fail and summary statistics become JSON null.

Fresh verification, 7 September 2026:

- `PYTHONPATH=src '/Users/daniel.tipton/Desktop/An app/.venv/bin/python'` with a `sys.meta_path` finder that raises on any import whose root is `mlx` or `mlx_lm`, then `pytest.main(['-q', '-p', 'no:cacheprovider', 'tests/test_lens_epsilon_diagnostic.py'])`: seven passed, exit 0. The same interpreter asserted no native modules imported. Initial five-test run also passed; no failing red test run is claimed.
- In the same guarded process with `HF_HOME='/Users/daniel.tipton/Desktop/An app/.cache/huggingface' HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1`, ran `prepare_fit` for `data/lens-fitting/qwen35-4b-agentic/manifest.json`, model `qwen35-4b`, revision `32f3e8ecf65426fc3306969496342d504bfa13f3`; then `prepare_plan` for `research/records/LENS-FIT-agentic-jacobian-2026-09-07/plan.json` and `validate_scope`. Passed, original plan hash `9fa35bb395d8c424b982d817f966204dc39bdb8d31f28ed9b9dd716d33dbc558`; sentinel NPZ absent. Evidence `diagnostic-green-02.log` (initial five tests in `diagnostic-green-01.log`). Primary heartbeat read immediately before both guarded test runs.
- Primary `.venv/bin/ruff check` and `ruff format --check` over exactly the three files: exit 0, all checks passed and three files formatted.

Tests cover exact independent schedule, unstable references still receiving cached comparisons, reference denominator asymmetry, explicit nonfinite failures/nulls, all-coordinate counts, actual shape-derived block accounting and hash chain, deadline before calls, partial timeout persistence, failed native-call accounting, and durable error status. Native numerical outcomes, actual memory peak, and wall time are unverified until the separately reviewed and announced run. Parent owns window/supervision/registration and independent source review. No commit or record from the original failed self-check is altered.
