# Corrected Jacobian derivative passes; full fit stopped at its cost boundary

8 September 2026. Branch codex/lens-fitting, corrected source5ad9fbe; patch LENS-POSITION-STEP.patch (SHA-256 59fabfe6f20028ffebedaa86af91015bf946115be95dd9bca067ad55ff030693). All work is in the persistent home-directory worktree. No shared production source was edited.

The numerical failure is resolved for the registered checks. The inherited step included every token's residual norm despite perturbing one position, producing a step about23% of that position's norm. The §15 sweep found a common plateau at c=0.03 relative to the selected position. Smaller coefficients show rising numerical noise. The original cache/reference and stability bounds were retained. All48 raw responses,18 grid comparisons and6 fresh cache comparisons are retained and audited.

Retry01 used the corrected production code, a newly frozen plan and another fresh real-checkpoint self-check. All six outcomes passed, with every reference stability outcome also passing. The plan hash is f641666af320bca2580f85112026728954757e3770396d6ab2086b84810526e3. Each layer's selected coefficient reaches reference, restore, broadcast, basis fitting and held-map validation through the same prepared state. Legacy plans remain reproducible. 41 CPU-only affected tests passed with native imports blocked; ruff and diff whitespace checks passed.

The fit entry point was then attempted. It stopped during the benchmark, before any full-basis fit or lens artifact. Nine benchmark observations were completed (through layer5 restore). At layer1, the best tested mode was batch8 broadcast: preparation7.2252seconds and0.402032seconds for8 directions at the worse of the two retained extremes. The existing formula projects400*(7.2252 + ceil(2560/8)*0.402032) =54,350.23seconds, or15.10hours at the400-position ceiling. At the150-position minimum the same conservative projection is5.66hours. These are concurrent, worst-context projections for layer1, not observed full-fit durations or an estimate for every layer.

The existing time_gate rejects anything above25minutes per layer. Larger batches cannot rescue this attempt under the current scheduler: its initial8GiB envelope doubles to16GiB at batch16, already beyond0.6 of the device working set; batch32 and64 are also excluded. This scales the whole envelope, including costs that do not grow with direction batch size. It is conservative, not a measurement of actual batch16 memory. A revised memory model and measured larger batches are the proposed next work; no claim is made that this alone will close the throughput gap.

The root task invoked the same pure time gate against the measured layer1 projection, verified that every larger batch is already excluded by the current before-call memory rule, recorded FIT-RETRY-01-COST-STOP.json, then sent SIGTERM only to its own supervisor. The complete31-layer benchmark was not finished; no expensive fit followed. The supervisor forwarded termination to its owned child, which exited and released its private lock. The generic end record's reason is null because it received a signal; the explicit cost-stop record is the authority for why it was stopped.

Retry duration252.97seconds. Last reported MLX peak5.6192GiB, beneath the6GiB allocation limit; this is a reported peak before termination, not a final exit-time sample. Kernel pressure remained normal. The Deputy's original PID84561 and primary lock nonce c08173cbedc54efda191ade2aee5ea83 remained present after our child and supervisor exited. No foreign process, lock or window was modified. Both attempts used the Director's explicit concurrent-load exception; the repository's standing default remains unchanged.

A logging limitation matters for reading these records: the fit CLI overwrites progress elapsed_s with total CLI elapsed time. Batch durations above were recovered from each observation's retained extremes, which is the same max reduction used by benchmark(), not from the cumulative field. The in-memory benchmark calculation is unaffected. This adjacent logging issue is recorded and not changed in this patch.

Decision required under requirements§3.3: keep the25-minute ceiling and rework batching/resource projection, or authorize a different cost budget/method. Recommendation: keep the numerical bounds and cost ceiling; first measure a better batched execution strategy under a separately stated memory envelope. The complete Jacobian lens is not yet fitted. Regression remains a separate instrument and was not substituted.
