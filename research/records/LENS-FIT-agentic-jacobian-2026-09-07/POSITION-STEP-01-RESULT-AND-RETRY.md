# Position-step diagnostic result and fit retry registration

The §15 sweep and fresh original self-check passed. All three layers have a plateau at c=0.03. The correction is epsilon = 0.03 * norm(selected float32 residual position) / norm(direction). The prior full-sequence normalization was oversized; the small end of the new grid also shows numerical noise. Only the original bounds determine passing.

The production fitter now carries the frozen coefficient through preparation, uncached reference, restored/broadcast cache responses, all-basis fitting and held-map validation. Legacy plans retain their original step exactly for reproducibility. No tolerance, direction, sampling count, convergence rule, model weights, cache arithmetic or default strategy changed.

Verification: position-local invariance and coefficient propagation tests; revised-plan round trip preserves fit/held samples and bounds; 41 CPU-only affected tests passed under a hook preventing native imports. An initial broad selection produced six explicit import-block failures in the native tests, then the CPU selection was run. Native numerical acceptance is the real-checkpoint self-check below and will be repeated through the revised production code before fitting.

Diagnostic: 48 raw response arrays, 18 grid stability comparisons, 6 cache/reference comparisons. All six cache comparisons and their fresh stability comparisons passed, zero failing coordinates. 23,616 block calls and 9,569,664 materialized block-tokens, hash chain and raw response hashes audited. Peak 3.86646 GiB; elapsed 699.05 seconds. Kernel pressure normal throughout; swap increased by 133.25 MiB during concurrent execution (not attributable to either process from this observation alone). Child exited, private lock removed, primary ownership preserved.

Retry01: one agentic all-layer Jacobian fit through scripts/lens_fit.py, using position-step-plan.json. The Director's concurrent-load exception persists for the retry; it uses its own private lock and does not modify the primary lock/window or signal foreign processes. MLX allocation limit6 GiB, cache0; original plan's8GiB before-call envelope and0.6-working-set bound retained. Original self-check runs fresh, then the batch8-first benchmark with conservative before-call larger-batch projections. The original25-minute-per-layer projected ceiling still stops the run for a Director choice if exceeded; no expensive full fit follows a failed gate. The outer supervisor budget is14 hours, covering at most31×25min plus overhead, and is only a backstop, not permission to bypass the per-layer gate. Pressure supervision terminates only this child on two consecutive critical samples or missing pressure telemetry. Output is a new lens path and fresh record directory; no artifact overwrite.
