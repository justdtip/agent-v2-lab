# Approved diagnostic — source ready, checkpoint run pending

Source commit0725738 adds only the standalone diagnostic module, CLI and seven pure tests. Source and operational review are complete; final wrapper is DIAGNOSTIC-01-LAUNCH-SOURCE-03.txt, mirrored in the persistent task workspace. The exact original four-scale protocol is registered in DIAGNOSTIC-01-REGISTRATION.md. This diagnostic does not substitute for requirements§15 or select a production step.

`design_specifications/pending/LENS-EPSILON-DIAGNOSTIC.patch` applies cleanly to the primary tree at e9be438; reverse-check passes on codex/lens-fitting. The patch adds the three source/test files and does not change existing numerical functions, capture, cache strategy or production gates. It has not been applied to primary by Codex.

Seven native-free tests, frozen-input preflight, Ruff and repository static checks passed. The same unchanged source was independently reviewed by GPT-6. The launcher uses the primary issue95 window mechanism, normal model lock, owned-child pressure supervision and an8-minute diagnostic budget. Its explicit invocation was refused by issue88's active training process20551. No native diagnostic measurement exists yet.

Next execution reads the latest primary heartbeat and verifies both the end line and absence of window/lock/mapped native processes. Do not clear the Deputy's state or stop its process. Existing approval remains valid; the remaining dependency is the box handoff. The original failed self-check records remain immutable. Regression is independently permitted by§15, and its next slot is mentioned in the Deputy's heartbeat.
