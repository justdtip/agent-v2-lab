# Verification

Fifteen file-only checks passed, including sealed source hashes and finished logs, shared endpoint/corpus/selector/precision, canonical sweep identity at the original step, invalid-control/duplicate/endpoint rejection, import refusal, the mixed-error halving counterexample, the global norm/RMS identity, and a quadratic counterexample showing why realized direction still needs a midpoint correction. All six generated outputs reproduce byte for byte. Existing outputs and changed sources are refused. No model library was loaded.

All eight remotely downloaded reports and producer scripts match the newly committed canonical c3abdbe record byte for byte. Two academic PDFs were downloaded from open primary-source locations and hashed. Source code and user relays were checked independently: the AD/FD endpoint is pre-final-normalization residual; the cited 3.9 fixture is not a real Gemma measurement; the whole-array step perturbs one coordinate; actual forward batches differ.

The PNG was opened and visually inspected. Axes, layer convention, measured step points, precision, selected population and failed-control status are legible. Error and cosine have separate axes/panels, and the illustrative error envelope is not drawn as if fitted to the measurements.

An independent source reviewer checked the diagnosis and protocol. Their amendments are incorporated: unchanged-input fractions use the intended perturbation support; native identity FD is checked against the realized displacement, not falsely required to equal the requested direction; the sign control flips exactly one vector; a graph-once directional result is compared against a sequential VJP wherever real-checkpoint equivalence is still unmeasured. A separate mathematical reviewer verified the ratio and scale identities and the midpoint caveat.

Only this new analysis directory is changed. No checkpoint or full map archive was downloaded; no running job, model precision, shared source, acceptance threshold or golden reference was changed. The proposed whole-float32 control remains unexecuted by Codex and belongs to the GPU owner's scheduled window.
