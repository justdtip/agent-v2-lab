# EXP-002 run lift, granted by the Director to the Chief, 2026-09-05 evening

Granted in the Chief's session, in answer to a three-way question: **EXP-002 run: granted.**
P6 extension to twenty ledger cases: not granted at this time (stays queued and costed in
WO-STAT-001 §2 and §5). EXP-001 extension (135 new points): not granted; withdrawn from the
Chief's recommendation on the World B bound.

Scope: one run of `agent-v2-state-swap` on `qwen35-4b`, policy `base`, all four arms, the
`jsweep` split and count defaults (EXP-001's cohort), `--exp001-artifact` pointing at the 4B
rerun's `sweep.json`, output `outputs/probes/state-swap-qwen35-4b-base-2026-09-05`, no
`--skip-preflight-check`. Prefills only; no Jacobians. The Head of Interpretability runs it; the
Deputy holds the lane and both watchers; the artifact cites the S3 commit's records child.
Preconditions: S3 committed; the Head's command checked by the Chief against EXP-002 §3 and
§4; the identity gate over three points and the chunking deviation recorded before any arm, and
a failed gate stops the run with the artifact written. Reading against §2 as ratified with B6.

## Attempt at 15:10:38 (not the Head's)

`outputs/probes/state-swap-qwen35-4b-base-2026-09-05/run.log`: started 15:10:38 with the Head's
prepared command verbatim, citing `24ca77b`, model loaded, failed at 15:10:42 at the first
render (the real template refuses a system-only prefix; see #59). The Head of Interpretability
did not start it: they were waiting for the Director's word in their own session, as this file
requires. Launched by: not the Deputy (stated plainly; their lane actions today were two preflight regenerations and test-suite runs). The evidence is that the traceback reached the Deputy pasted into a message from the Director, ending with the Director's own shell prompt, so the run appears to have been launched from the Director's terminal with the Head's command and within the lift's scope. **Confirmed by the Director to the Chief (2026-09-05 evening): the Director launched it themselves from their terminal.** No lift is needed for the Director's own action and no breach occurred; the entry records who ran a model on the lane, as the discipline requires. The Head's condition for the rerun is unchanged: it starts on the Director's word in the Head's own session and on the gated fix, unless the Director runs it themselves again.
Recorded because the lift names the Head as the runner and a model ran on the lane outside the
sanctioned actor; the failure itself was cheap and is fixed. The lift stands for the rerun,
which starts on the Director's word in the Head's session and on the gated fix.

## Gate record (Chief, 2026-09-05 late evening)

The first-render fix (worktree wt-template-prefix on 24ca77b; patch fix-template-prefix.patch; 8 files, +580/-28) passed the Chief's gate on a read of every hunk: render_rows user-turn guard with the narrower real rule documented; build_prompt precondition docstring; runner comment; state_swap head-block discovery (_rendered_boundaries returns the head count; refusals before the first renderable prefix define the head, after it are hard errors; tool messages inside the head refused; _spans_by_message maps message head_count - 1 + j to chunk j; select_points refuses hidden indices inside the head and records head_messages); tests: stand-in reproduces the template's refusals with trim, real-tokenizer fixture with a loud narrow skip, four writer-backed tests, a stand-in-only table test, and a meta-guard that identifies the stand-in-versus-writer comparison by its behaviour and requires an equality over a non-degenerate table. The four account corrections are present in the code. Verdict: READY TO COMMIT as one commit with the corrections. The Deputy commits. Non-blocking note recorded: forward_schedule requires head_messages keys that no pre-change point carries; none exist.
