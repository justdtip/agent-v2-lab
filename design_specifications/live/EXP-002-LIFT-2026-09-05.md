# EXP-002 run lift, granted by the Director to the Chief, 2026-09-05 evening

Granted in the Chief's session, in answer to a three-way question: **EXP-002 run: granted.**
P6 extension to twenty ledger cases: not granted at this time (stays queued and costed in
WO-STAT-001 §2 and §5). EXP-001 extension (135 new points): not granted; withdrawn from the
Chief's recommendation on the World B bound.

Scope: one run of `agent-v2-state-swap` on `qwen35-4b`, policy `base`, all four arms, the
`jsweep` split and count defaults (EXP-001's cohort), `--exp001-artifact` pointing at the 4B
rerun's `sweep.json`, output `outputs/probes/state-swap-qwen35-4b-base-2026-09-05`, no
`--skip-preflight-check`. Prefills only; no Jacobians. The Head of Interpretability runs it; the
Deputy holds the lane and both watchers; the artifact cites the S3 commit's records child.
Preconditions: S3 committed; the Head's command checked by the Chief against EXP-002 §3 and
§4; the identity gate over three points and the chunking deviation recorded before any arm, and
a failed gate stops the run with the artifact written. Reading against §2 as ratified with B6.
