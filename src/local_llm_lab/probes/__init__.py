"""Interpretability probe suite (``research/representation_probes.md``).

Phase one: shared activation-capture infrastructure (:mod:`capture`), the exact adapter diff
(P5, :mod:`adapter_delta`), linear state probes (P2, :mod:`state_probe`), and the measurement
half of the assistant axis (P1, :mod:`assistant_axis`).
"""
