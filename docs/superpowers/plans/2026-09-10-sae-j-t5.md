# SAE-J T5 implementation plan

**Goal:** Produce measured WS-D cross-path registrations for exact capture cells.
**Spec:** `design_specifications/pending/SAE-J-BRIDGE-ORDER-2026-09-08.md`, T5 at `2e556a0`.

The supplied T5 order is the approved design. Work stays in this checkout, uses CPU fixtures,
and does not execute a checkpoint, download data, or write the Chief's domain statement.

1. Add failing tests in `tests/test_measure_pairings.py`: an actual tiny Torch decoder and
   synthetic workspace capture, bitwise reproduction and perturbation, width dependence,
   all five named refusals, and real table/runner interoperability.
2. Extract the runner's capture/cell validation into `_capture_cells` in `device_bridge.py`.
   Both callers use it; A2 retains its pairing and domain-of-validity guards afterward.
3. Implement `probes/measure_pairings.py` and `scripts/measure_pairings.py`. Validate explicit
   checkpoint manifest, admission, capture seals, cells, and layers before loading a model.
   Use replicated unpadded input IDs, eager float32 decoder block outputs, and compare the
   width-1 bytes before computing any cross-path term. Preserve every ordered chunk term.
4. Permit registration lists in the existing table validator. Bind generated registrations
   to cells and capture digests in underscore provenance so equal lengths on different prompts
   cannot transfer a measurement. Preserve all eleven pair fields and the three wrapper fields.
5. Run focused CPU checks, inspect the diff, run the affected bridge/admission checks plus
   lint, and commit only reviewed files after pytest's own exit code is zero. Record the
   checked set and any remaining card-only validation in the implementation report.
